var answersElem = document.getElementById('answers-index');
var paginationElem = document.getElementById('answers-pagination');
answersElem.innerHTML = "<%= j(render partial: "/answers/another_user_answer", collection: @other_users_answers, as: :answer, locals: { question: @question }) %>";
paginationElem.innerHTML = "<%= j(render "shared/pagy", pagy: @pagy ) %>";
Pagy.init(paginationElem);
var pointElems = document.querySelectorAll(".trix-content");
MathJax.texReset([0]);
MathJax.typeset(pointElems);
