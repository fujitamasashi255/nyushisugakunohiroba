var questionsElem = document.getElementById('questions-index');
var paginationElem = document.getElementById('questions-pagination');
var scrollToElem = document.querySelector('.sort-links');
questionsElem.innerHTML = "<%= j(render partial: '/questions/question', layout: '/shared/questions/question_layout', collection: @questions, as: :question, locals: { question_id_to_answer_id_hash_of_user: @question_id_to_answer_id_hash_of_user }) %>";
paginationElem.innerHTML = "<%= j(render "shared/pagy", pagy: @pagy ) %>";
Pagy.init(paginationElem);
scrollToElem.scrollIntoView();
