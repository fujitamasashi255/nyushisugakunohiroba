- content_for(:title, t('.title'))

.container
  = render "form", {question: @question, departments: @departments, university: @university, units: @units, tex: @tex, checkbox_departments: @checkbox_departments}
